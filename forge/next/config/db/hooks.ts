import { Logger } from "@repo/core-domain";

(function createHooksForAggregateRoots(): void {
  // HooksManager.addHook("afterUserCreated");
  Logger.info("[HOOKS]: DB hooks initialised 🎉");
})();
