Nextjs + Fastify Boilerplate

PNPM workspaces are used to manage the monorepo.

## Features
- Fastify
- Next.js
- TypeScript
- Docker

RUN
docker-compose up <service_name> --build --force-recreate -V
